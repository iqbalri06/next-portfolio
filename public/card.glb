// This should be a valid GLB file. Since we can't include binary content directly in this response, 
// you can use a placeholder GLB file or download a simple one from:
// https://market.pmnd.rs/model/id-card
// or create a simple one using Blender and export as GLB
